# User Steering Rules

Your personal rules and preferences. Poseidon never auto-modifies this file.
Add rules here that reflect how YOU want your AI to behave.

THIS FILE MUST EXIST even if empty — it is force-loaded at every session start.
If deleted, the loading pipeline skips user rules entirely.

---

<!-- Add your personal rules below. Examples:

**Always use Tailscale SSH, never regular SSH.**
Reason: All my servers are on a Tailscale mesh. Regular SSH won't reach them.

**Prefer Perplexity for quick research.**
Reason: Faster than WebSearch for factual lookups.

**Never commit .env files.**
Reason: Contains production secrets.

-->
